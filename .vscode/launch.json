{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "TaskbarAudioSpectrum",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false
    }
  ]
}
